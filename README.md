# per-sla-monitoring

Cette branche contient le code du programme de rejeu de données, il part d'un dataset, et envoie les données petit à petit vers le broker mqtt.

## lancer le projet

```bash
docker compose up -d
```